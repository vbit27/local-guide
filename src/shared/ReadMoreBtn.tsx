import React from 'react';
import classes from './ReadMoreBtn.module.css';

const ReadMoreBtn: React.FC = () => {
  return <button className={classes.button}>{'Read more  >'} </button>;
};

export default ReadMoreBtn;
